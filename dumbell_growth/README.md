Pls download both .xml files and the .py file in the same folder to run it. To prepare the .xml files, you have to modify abaquas.py (more details later) using terminal, then you do the conversion of .inp file to .xml also in terminal by "dolfin-convert input_file.inp output_file.xml" *Note to myself: copy the inp file export from abaquas into folder in Ubuntu system, open terminal in Ubuntu, locate "FEniCS_test" and use the dolfin-convert code to convert inp to xml, this will generate two xml files, bring them back to the windows folder and put them into the same folder as the model.py script. But actually i dont think this process has to go through ubuntu's folder.
